apiVersion: v1
kind: Service
metadata:
  name: silhouette
  namespace: world
spec:
  selector:
    app: silhouette
  ports:
    - protocol: TCP
      port: 8443
      targetPort: 8443
  type: ClusterIP
